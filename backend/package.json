{
  "name": "@voip-monitor/backend",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "npm run build && node --test test/*.test.mjs",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@voip-monitor/shared": "0.1.0",
    "ssh2": "^1.17.0",
    "zod": "4.6.5"
  },
  "devDependencies": {
    "@types/ssh2": "^1.15.6"
  }
}
